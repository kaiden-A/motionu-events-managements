/* =========================================================
   INIT
   ========================================================= */
(function init(){
  contextEventId = defaultEventId();
  setPage('dashboard');
})();
